# Neon Postgres
DATABASE_URL=postgresql://user:pass@host/dbname?sslmode=require

# Auth.js
AUTH_SECRET=  # Generate with: openssl rand -base64 32
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=

# Optional: Auth.js trust host (for Vercel)
AUTH_TRUST_HOST=true
