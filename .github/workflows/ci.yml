name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write

jobs:
  # =============================================================================
  # Frontend — Lint + Typecheck + Build
  # CUSTOMIZE: Uncomment and update for your frontend stack
  # =============================================================================
  # lint-frontend:
  #   name: Lint (Frontend)
  #   runs-on: ubuntu-latest
  #   defaults:
  #     run:
  #       working-directory: src
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - name: Setup Node
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: 22
  #         cache: "npm"
  #         cache-dependency-path: src/package-lock.json
  #
  #     - name: Install dependencies
  #       run: npm ci
  #
  #     - name: Lint
  #       run: npm run lint
  #
  # typecheck-frontend:
  #   name: Typecheck (Frontend)
  #   runs-on: ubuntu-latest
  #   defaults:
  #     run:
  #       working-directory: src
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - name: Setup Node
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: 22
  #         cache: "npm"
  #         cache-dependency-path: src/package-lock.json
  #
  #     - name: Install dependencies
  #       run: npm ci
  #
  #     - name: Typecheck
  #       run: npm run typecheck
  #
  # build-frontend:
  #   name: Build (Frontend)
  #   runs-on: ubuntu-latest
  #   needs: [lint-frontend, typecheck-frontend]
  #   defaults:
  #     run:
  #       working-directory: src
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - name: Setup Node
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: 22
  #         cache: "npm"
  #         cache-dependency-path: src/package-lock.json
  #
  #     - name: Install dependencies
  #       run: npm ci
  #
  #     - name: Build
  #       run: npm run build

  # =============================================================================
  # Backend — Lint + Typecheck + Test + Build
  # CUSTOMIZE: Uncomment and update for your backend stack
  # =============================================================================
  # lint-backend:
  #   name: Lint (Backend)
  #   runs-on: ubuntu-latest
  #   defaults:
  #     run:
  #       working-directory: src
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - name: Setup Node
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: 22
  #         cache: "npm"
  #         cache-dependency-path: src/package-lock.json
  #
  #     - name: Install dependencies
  #       run: npm ci
  #
  #     - name: Lint
  #       run: npm run lint
  #
  # typecheck-backend:
  #   name: Typecheck (Backend)
  #   runs-on: ubuntu-latest
  #   defaults:
  #     run:
  #       working-directory: src
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - name: Setup Node
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: 22
  #         cache: "npm"
  #         cache-dependency-path: src/package-lock.json
  #
  #     - name: Install dependencies
  #       run: npm ci
  #
  #     - name: Typecheck
  #       run: npm run typecheck
  #
  # test-backend:
  #   name: Test (Backend)
  #   runs-on: ubuntu-latest
  #   needs: [lint-backend, typecheck-backend]
  #   defaults:
  #     run:
  #       working-directory: src
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - name: Setup Node
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: 22
  #         cache: "npm"
  #         cache-dependency-path: src/package-lock.json
  #
  #     - name: Install dependencies
  #       run: npm ci
  #
  #     - name: Run Tests
  #       run: npm test
  #       env:
  #         NODE_ENV: test
  #
  # build-backend:
  #   name: Build (Backend)
  #   runs-on: ubuntu-latest
  #   needs: [lint-backend, typecheck-backend]
  #   defaults:
  #     run:
  #       working-directory: src
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - name: Setup Node
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: 22
  #         cache: "npm"
  #         cache-dependency-path: src/package-lock.json
  #
  #     - name: Install dependencies
  #       run: npm ci
  #
  #     - name: Build
  #       run: npm run build

  # =============================================================================
  # Auto-Label PRs on CI Results
  # CUSTOMIZE: Update the 'needs' array to match your active jobs
  # =============================================================================
  # label-pr:
  #   name: Label PR
  #   runs-on: ubuntu-latest
  #   needs: [lint-backend, typecheck-backend, test-backend, build-backend]
  #   if: always() && github.event_name == 'pull_request'
  #   steps:
  #     - name: Add CI Pass label
  #       if: |
  #         needs.lint-backend.result == 'success' &&
  #         needs.typecheck-backend.result == 'success' &&
  #         needs.test-backend.result == 'success' &&
  #         needs.build-backend.result == 'success'
  #       uses: actions/github-script@v7
  #       with:
  #         script: |
  #           await github.rest.issues.addLabels({
  #             owner: context.repo.owner,
  #             repo: context.repo.repo,
  #             issue_number: context.issue.number,
  #             labels: ['CI Pass']
  #           });
  #
  #     - name: Add CI Failure label
  #       if: |
  #         needs.lint-backend.result == 'failure' ||
  #         needs.typecheck-backend.result == 'failure' ||
  #         needs.test-backend.result == 'failure' ||
  #         needs.build-backend.result == 'failure'
  #       uses: actions/github-script@v7
  #       with:
  #         script: |
  #           await github.rest.issues.addLabels({
  #             owner: context.repo.owner,
  #             repo: context.repo.repo,
  #             issue_number: context.issue.number,
  #             labels: ['CI Failure']
  #           });

  # =============================================================================
  # Required Status Check (summary job for branch protection)
  # CUSTOMIZE: Update 'needs' to match your active jobs
  # =============================================================================
  # ci-success:
  #   name: CI Success
  #   runs-on: ubuntu-latest
  #   needs: [lint-backend, typecheck-backend, test-backend, build-backend]
  #   if: always()
  #   steps:
  #     - name: Check all jobs passed
  #       run: |
  #         if [[ "${{ needs.lint-backend.result }}" != "success" ]] || \
  #            [[ "${{ needs.typecheck-backend.result }}" != "success" ]] || \
  #            [[ "${{ needs.test-backend.result }}" != "success" ]] || \
  #            [[ "${{ needs.build-backend.result }}" != "success" ]]; then
  #           echo "One or more jobs failed"
  #           exit 1
  #         fi
  #         echo "All CI checks passed!"

  # =============================================================================
  # Placeholder job (remove once you uncomment real jobs above)
  # GitHub Actions requires at least one job to be valid
  # =============================================================================
  placeholder:
    name: Template CI (configure me)
    runs-on: ubuntu-latest
    steps:
      - name: Reminder
        run: echo "Configure CI by uncommenting jobs in .github/workflows/ci.yml"
